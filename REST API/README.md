# API for Slovak business bulletin

Project built using Python and Django framework.

Code snippets for selected REST API endpoints for business bulletin database. Raw SQL is implemented in v1 and ORM is implemented in v2.
